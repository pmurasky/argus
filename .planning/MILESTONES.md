# Milestones

## v1.0 — Foundation (Complete)

**Shipped:** 2026-06-15
**Phases:** 1–17 (tracked via task list, pre-GSD structure)

### What shipped

- CLI: `init`, `generate` (--dry-run, --check), `packs list/show`, `platforms list`, `validate`
- 5 built-in packs: `atomic-commit`, `tdd`, `solid`, `code-quality`, `pre-commit`
- 4 platform adapters: `claude`, `opencode`, `copilot`, `cursor`
- Custom pack loading from `.argus/packs/`
- AGENTS.md generated for all platforms
- PyPI publish CI workflow (tag-triggered)
- Published as `argus-standards` v0.1.1
- 91 tests passing

### Key outcomes

- Argus dogfoods itself — CLAUDE.md, AGENTS.md, .claude/rules/ are all generated by argus
- Clean SOLID architecture: new platform = one file, no existing changes
- Two-level pack search (custom → built-in) enables project overrides

---
