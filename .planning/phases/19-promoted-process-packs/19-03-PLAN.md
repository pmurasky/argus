---
phase: 19-promoted-process-packs
plan: 03
type: execute
wave: 3
depends_on: [02]
files_modified:
  - tests/integration/test_generate.py
  - argus/packs/documentation-standards/examples.md
autonomous: true
requirements: [PACK-04]
must_haves:
  truths:
    - "documentation-standards pack appears in `argus packs list` output"
    - "`argus packs show documentation-standards` renders the pack content"
    - "`argus generate` with documentation-standards pack writes content into .claude/rules/documentation-standards.md"
    - "documentation-standards pack examples use generic class/method names, not Argus-specific ones"
  artifacts:
    - path: "tests/integration/test_generate.py"
      provides: "Three integration tests for the documentation-standards pack"
      contains: "def test_documentation_standards_pack_generate_injects_content"
    - path: "argus/packs/documentation-standards/examples.md"
      provides: "Generic docstring examples (DataLoader, list_items)"
      contains: "class DataLoader"
  key_links:
    - from: "tests/integration/test_generate.py"
      to: ".claude/rules/documentation-standards.md"
      via: "generate then read rules file"
      pattern: "claude/rules/documentation-standards.md"
---

<objective>
Add integration test coverage for the `documentation-standards` pack, then generalize the
Argus-specific class/method names in its examples.md (PackLoader, available_packs, _load_one,
Pack, .argus.yml) so the pack is usable in any project.

Purpose: Satisfy PACK-04 — `documentation-standards` pack works end-to-end through the CLI and
contains no Argus-specific references.
Output: 3 new integration tests; one content fix (examples.md).

NOTE: This plan depends on Plan 02 because all three Phase 19 plans append test functions to the
same file (tests/integration/test_generate.py). It runs in Wave 3 to avoid an append conflict.
</objective>

<execution_context>
@/home/peter/.claude/get-shit-done/workflows/execute-plan.md
@/home/peter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-promoted-process-packs/19-CONTEXT.md
@.planning/phases/19-promoted-process-packs/19-RESEARCH.md

<interfaces>
<!-- Pack instruction content lands in .claude/rules/documentation-standards.md, NOT CLAUDE.md. -->
<!-- The generate assertion MUST read .claude/rules/documentation-standards.md. -->

<!-- `packs list` and `packs show` take NO --project-root flag. Only `generate` does. -->

<!-- Key phrase "imperative mood" is chosen for the generate/show assertions because it lives -->
<!-- in instructions.md Docstring Style and is NOT changed in this phase — only examples.md is -->
<!-- edited here. Do NOT key on PackLoader/available_packs (those are being replaced). -->

Follow the exact test pattern established in Plans 01 and 02 (isolated CONFIG constant + three
test functions appended to the end of the file).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add three integration tests for the documentation-standards pack</name>
  <files>tests/integration/test_generate.py</files>
  <read_first>
    - tests/integration/test_generate.py (current state — Plans 01 and 02 already added type-safety and error-handling tests; append after the last test)
    - argus/packs/documentation-standards/instructions.md (confirm the phrase "imperative mood" appears in Docstring Style — the chosen key phrase, located in a file NOT edited this plan)
  </read_first>
  <behavior>
    - Test 1 (packs list): `argus packs list` exits 0 and output contains "documentation-standards"
    - Test 2 (packs show): `argus packs show documentation-standards` exits 0 and output contains "imperative mood"
    - Test 3 (generate): `argus generate --project-root <tmp>` with a documentation-standards+claude config
      exits 0 and .claude/rules/documentation-standards.md contains "imperative mood"
  </behavior>
  <action>
    Append a DOCUMENTATION_STANDARDS_CONFIG constant and three test functions to the END of
    tests/integration/test_generate.py. Do NOT add new imports. Do NOT modify FULL_CONFIG.

    Add this constant near the other config constants:

    ```python
    DOCUMENTATION_STANDARDS_CONFIG = """\
    packs:
      - documentation-standards
    platforms:
      - claude
    """
    ```

    Add these three test functions at the end of the file:

    ```python
    def test_documentation_standards_pack_appears_in_packs_list():
        runner = CliRunner()
        result = runner.invoke(main, ["packs", "list"])
        assert result.exit_code == 0
        assert "documentation-standards" in result.output


    def test_documentation_standards_pack_show_renders_content():
        runner = CliRunner()
        result = runner.invoke(main, ["packs", "show", "documentation-standards"])
        assert result.exit_code == 0
        assert "imperative mood" in result.output


    def test_documentation_standards_pack_generate_injects_content(tmp_path):
        (tmp_path / ".argus.yml").write_text(DOCUMENTATION_STANDARDS_CONFIG)
        runner = CliRunner()
        result = runner.invoke(main, ["generate", "--project-root", str(tmp_path)])
        assert result.exit_code == 0, result.output
        assert "imperative mood" in (tmp_path / ".claude/rules/documentation-standards.md").read_text()
    ```

    Key phrase "imperative mood" is chosen because it lives in instructions.md (not examples.md),
    which is NOT edited this plan — it survives the Task 2 content fix.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/integration/test_generate.py -k documentation_standards -x</automated>
  </verify>
  <acceptance_criteria>
    - tests/integration/test_generate.py contains `def test_documentation_standards_pack_appears_in_packs_list`
    - tests/integration/test_generate.py contains `def test_documentation_standards_pack_show_renders_content`
    - tests/integration/test_generate.py contains `def test_documentation_standards_pack_generate_injects_content`
    - tests/integration/test_generate.py contains `DOCUMENTATION_STANDARDS_CONFIG`
    - tests/integration/test_generate.py contains `.claude/rules/documentation-standards.md`
    - `.venv/bin/pytest tests/integration/test_generate.py -k documentation_standards -x` exits 0
  </acceptance_criteria>
  <done>All three documentation-standards integration tests exist and pass.</done>
</task>

<task type="auto">
  <name>Task 2: Generalize Argus-specific names in documentation-standards examples</name>
  <files>argus/packs/documentation-standards/examples.md</files>
  <read_first>
    - argus/packs/documentation-standards/examples.md (full file — contains PackLoader, available_packs, Pack, _load_one, .argus.yml)
    - .claude/rules/documentation-standards.md (source of truth for intent — keep meaning identical, only example names become generic)
  </read_first>
  <action>
    Replace the entire body of argus/packs/documentation-standards/examples.md with the
    generalized version below. This generalizes PackLoader → DataLoader,
    available_packs → list_items, Pack → Record, and removes the `.argus.yml` literal. Keep the
    same structure, headings, and teaching intent.

    ```markdown
    ## Documentation Standards Examples

    ### Correct

    ```python
    class DataLoader:
        """Load records from built-in and custom search paths."""

    def list_items(self) -> list[str]:
        """Return sorted list of all discoverable item names."""

    @main.command()
    def generate(...):
        """Generate platform-specific files from config."""

    # YAML safe_load returns Any — typed immediately below
    data: dict[str, Any] = yaml.safe_load(path.read_text())
    ```

    ### Incorrect

    ```python
    class DataLoader:
        """DataLoader class."""          # restates the name

    def list_items(self):
        """Gets the available items."""  # wrong mood, explains what

    # increment the counter
    i += 1                               # explains what, not why
    ```

    ### Exempt (no docstring needed)

    ```python
    def __init__(self, project_root: Path) -> None: ...
    def _load_one(self, name: str) -> Record: ...
    ```
    ```

    After the edit, the strings `PackLoader`, `available_packs`, `.argus.yml`, and the standalone
    return type `Pack` must NOT appear anywhere in the documentation-standards pack directory.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/integration/test_generate.py -k documentation_standards -x && ! grep -rqE "PackLoader|available_packs|\.argus\.yml" argus/packs/documentation-standards/</automated>
  </verify>
  <acceptance_criteria>
    - `grep -r "PackLoader" argus/packs/documentation-standards/` returns nothing
    - `grep -r "available_packs" argus/packs/documentation-standards/` returns nothing
    - `grep -r ".argus.yml" argus/packs/documentation-standards/` returns nothing
    - argus/packs/documentation-standards/examples.md contains `class DataLoader`
    - argus/packs/documentation-standards/examples.md contains `def list_items`
    - argus/packs/documentation-standards/examples.md contains `-> Record`
    - `.venv/bin/pytest tests/integration/test_generate.py -k documentation_standards -x` exits 0
  </acceptance_criteria>
  <done>No Argus-specific name remains in the documentation-standards pack; tests stay green.</done>
</task>

</tasks>

<verification>
- `.venv/bin/pytest tests/integration/test_generate.py -k documentation_standards -x` exits 0
- `grep -rE "PackLoader|available_packs" argus/packs/documentation-standards/` returns nothing
- `.venv/bin/pytest` full suite green — all 9 new Phase 19 tests plus existing tests pass
</verification>

<success_criteria>
- documentation-standards pack passes packs list / packs show / generate integration tests
- documentation-standards examples use only generic names
- PACK-04 verified end-to-end through the CLI
- All three Phase 19 packs (PACK-02, PACK-03, PACK-04) fully covered
</success_criteria>

<output>
After completion, create `.planning/phases/19-promoted-process-packs/19-03-SUMMARY.md`
</output>
