---
phase: 23-python-javascript-framework-packs
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/integration/test_generate.py
  - argus/packs/fastapi/pack.yml
  - argus/packs/fastapi/instructions.md
  - argus/packs/fastapi/checklist.md
  - argus/packs/fastapi/examples.md
autonomous: true
requirements: [FWRK-01]

must_haves:
  truths:
    - "User can add `fastapi` to .argus.yml packs and `argus generate` injects FastAPI rules into .claude/rules/fastapi.md"
    - "`argus packs list` shows fastapi under the framework category"
    - "`argus packs show fastapi` renders the fastapi instructions content"
  artifacts:
    - path: "argus/packs/fastapi/pack.yml"
      provides: "fastapi pack manifest with category: framework"
      contains: "category: framework"
    - path: "argus/packs/fastapi/instructions.md"
      provides: "FastAPI rules across 4 sections; contains key phrase APIRouter"
      contains: "APIRouter"
    - path: "argus/packs/fastapi/checklist.md"
      provides: "Pre-commit checklist for FastAPI"
      min_lines: 8
    - path: "argus/packs/fastapi/examples.md"
      provides: "Avoid/Prefer side-by-side FastAPI examples"
      contains: "**Prefer**"
    - path: "tests/integration/test_generate.py"
      provides: "FASTAPI_CONFIG + 3 integration tests"
      contains: "FASTAPI_CONFIG"
  key_links:
    - from: "tests/integration/test_generate.py"
      to: "argus/packs/fastapi/instructions.md"
      via: "packs show / generate assert key phrase APIRouter"
      pattern: "APIRouter"
    - from: "argus/packs/fastapi/pack.yml"
      to: "argus.loader auto-discovery"
      via: "directory under argus/packs/ discovered by available_packs()"
      pattern: "name: fastapi"
---

<objective>
Ship the `fastapi` framework pack: a four-file pack directory under `argus/packs/fastapi/` plus
three integration tests in `tests/integration/test_generate.py`. The pack injects FastAPI-specific
rules (async patterns, Pydantic v2 models, dependency injection, router organization) into agent
instruction files.

Purpose: Satisfies FWRK-01 — users building FastAPI APIs get precise, actionable agent rules.
Output: `argus/packs/fastapi/{pack.yml,instructions.md,checklist.md,examples.md}` and 3 passing
integration tests following the KOTLIN_CONFIG precedent.
</objective>

<execution_context>
@/home/peter/.claude/get-shit-done/workflows/execute-plan.md
@/home/peter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-python-javascript-framework-packs/23-CONTEXT.md
@.planning/phases/23-python-javascript-framework-packs/23-RESEARCH.md

# Reference packs (structural template — copy structure, replace content)
@argus/packs/python/pack.yml
@argus/packs/kotlin/instructions.md
@argus/packs/kotlin/checklist.md
@argus/packs/security/examples.md

# Test pattern (replicate KOTLIN_CONFIG block verbatim, swap fastapi)
@tests/integration/test_generate.py

<interfaces>
<!-- How the pack system works — executor needs no codebase exploration. -->

pack.yml manifest format (consumed by argus/loader.py PackLoader._read()):
```yaml
name: fastapi
description: <one line>
category: framework
requires: []
platforms: [all]
```

`argus packs show fastapi` renders instructions.md ONLY (the `instructions` field).
The key phrase asserted by the show + generate tests MUST appear verbatim in instructions.md
(NOT in checklist.md or examples.md). Chosen key phrase: APIRouter.

`argus generate` with packs: [fastapi], platforms: [claude] writes pack content to
`.claude/rules/fastapi.md` in the project root.

Loader auto-discovers any directory under `argus/packs/` — no registration, no code changes.
</interfaces>

<constraints>
- Test runner is `.venv/bin/pytest` (NOT bare pytest).
- DO NOT modify tests/test_packs.py — its VALID_CATEGORIES and REQUIRED_PACKS must not change
  (framework/language packs are validated only in test_generate.py).
- FastAPI pack is ADDITIVE — it must NOT duplicate python pack content. FORBIDDEN strings in
  instructions.md: mypy, PEP 8, f-string, pathlib, "type hint" (those belong to python pack).
- Pydantic scope is FastAPI endpoint models only. FORBIDDEN: BaseSettings, discriminated union,
  custom serializer (general Pydantic — out of scope per CONTEXT.md).
- Pydantic v2 syntax only: model_config, model_validator, field_validator, Field. No v1 shims.
- Async-first is a HARD RULE: always `async def` for route handlers; sync `def` only for
  CPU-bound work in a thread pool.
</constraints>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add FASTAPI_CONFIG and 3 failing integration tests (RED)</name>
  <files>tests/integration/test_generate.py</files>
  <read_first>
    - tests/integration/test_generate.py (read the KOTLIN_CONFIG block at lines 370-400 — replicate it verbatim, swapping kotlin->fastapi)
  </read_first>
  <behavior>
    - test_fastapi_pack_appears_in_packs_list: `packs list` exit 0 and output contains "fastapi"
    - test_fastapi_pack_show_renders_content: `packs show fastapi` exit 0 and output contains "APIRouter"
    - test_fastapi_pack_generate_injects_content: generate with FASTAPI_CONFIG writes
      .claude/rules/fastapi.md containing "APIRouter"
    All 3 MUST fail initially (pack directory does not exist yet).
  </behavior>
  <action>
Append to the END of tests/integration/test_generate.py (after the existing kotlin tests at line 400) the following block, replicating the KOTLIN_CONFIG pattern exactly:

```python


FASTAPI_CONFIG = """\
packs:
  - fastapi
platforms:
  - claude
"""


def test_fastapi_pack_appears_in_packs_list():
    """Given packs list is invoked, fastapi appears in the output."""
    runner = CliRunner()
    result = runner.invoke(main, ["packs", "list"])
    assert result.exit_code == 0
    assert "fastapi" in result.output


def test_fastapi_pack_show_renders_content():
    """Given packs show fastapi is invoked, APIRouter appears in the output."""
    runner = CliRunner()
    result = runner.invoke(main, ["packs", "show", "fastapi"])
    assert result.exit_code == 0
    assert "APIRouter" in result.output


def test_fastapi_pack_generate_injects_content(tmp_path):
    """Given a fastapi+claude config, generate writes content to .claude/rules/fastapi.md."""
    (tmp_path / ".argus.yml").write_text(FASTAPI_CONFIG)
    runner = CliRunner()
    result = runner.invoke(main, ["generate", "--project-root", str(tmp_path)])
    assert result.exit_code == 0, result.output
    assert "APIRouter" in (tmp_path / ".claude/rules/fastapi.md").read_text()
```

Do NOT use `from typing import List/Dict` — file already uses CliRunner and main imports; reuse them. Do NOT add fastapi to FULL_CONFIG or any shared constant. Do NOT touch tests/test_packs.py.

Run the tests and CONFIRM they fail (RED) because argus/packs/fastapi/ does not exist yet.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/integration/test_generate.py -k fastapi -x -q; test $? -ne 0 && echo "RED confirmed"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "FASTAPI_CONFIG" tests/integration/test_generate.py` returns at least 2
    - `grep -c "def test_fastapi_pack" tests/integration/test_generate.py` returns 3
    - `grep "APIRouter" tests/integration/test_generate.py` matches (key phrase in show + generate asserts)
    - `git diff --name-only` does NOT include tests/test_packs.py
    - `.venv/bin/pytest tests/integration/test_generate.py -k fastapi -x -q` exits NON-zero (RED — pack not authored yet)
  </acceptance_criteria>
  <done>3 fastapi tests exist in test_generate.py and all fail because the pack directory is absent.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author the fastapi pack files (GREEN)</name>
  <files>argus/packs/fastapi/pack.yml, argus/packs/fastapi/instructions.md, argus/packs/fastapi/checklist.md, argus/packs/fastapi/examples.md</files>
  <read_first>
    - argus/packs/python/pack.yml (manifest structure to mirror)
    - argus/packs/kotlin/instructions.md (4-section H2 density + "Red Flags — Stop and Correct" closer)
    - argus/packs/kotlin/checklist.md (checklist format)
    - argus/packs/security/examples.md (Avoid/Prefer side-by-side format)
  </read_first>
  <action>
Create the directory argus/packs/fastapi/ with four files.

**argus/packs/fastapi/pack.yml** (verbatim):
```yaml
name: fastapi
description: Async-first routing, Pydantic v2 models, and dependency injection patterns
category: framework
requires: []
platforms: [all]
```

**argus/packs/fastapi/instructions.md** — 4 H2 sections, 4-6 bullet rules each, ending with a "## Red Flags — Stop and Correct" section. The string `APIRouter` MUST appear in the Router Organization section. Write concrete, FastAPI-specific rules. Use exactly these four sections:

```markdown
# FastAPI

## Async Patterns
- Declare every route handler `async def` — FastAPI runs them on the event loop without blocking
- Use plain `def` ONLY for CPU-bound work; FastAPI offloads sync handlers to a thread pool
- Never call blocking I/O (requests, time.sleep, blocking DB drivers) inside an `async def` handler
- Use `async`-native libraries (httpx, asyncpg, databases) for I/O inside async handlers
- Stream large responses with `async for` and a generator rather than buffering in memory

## Pydantic Models
- Define a request model and a response model per endpoint as the typed I/O boundary
- Use Pydantic v2 syntax: `model_config`, `model_validator`, `field_validator` (never v1 `Config` class or `@validator`)
- Use `Field()` for API-level validation: `min_length`, `max_length`, `ge`/`le`, `description`, `alias`
- Set serialization behavior with `model_config = ConfigDict(populate_by_name=True, from_attributes=True)`
- Express cross-field rules with `@model_validator(mode="after")`, single-field rules with `@field_validator`
- Declare `response_model=` on the route so output is filtered and documented

## Dependency Injection
- Inject shared resources (DB sessions, current user, settings) with `Depends()`
- Write dependencies as functions or callables that `yield` for setup/teardown (e.g., DB session lifecycle)
- Depend on abstractions so tests override dependencies via `app.dependency_overrides`
- Never instantiate services or open connections inside a route body — inject them
- Layer dependencies: a higher-level dependency may itself declare `Depends()` on lower ones

## Router Organization
- Create one `APIRouter` per feature/domain; never inline all routes in `main.py`
- Set `prefix=` and `tags=[]` at the `APIRouter` level, not on individual endpoints
- Assemble the app by calling `app.include_router(...)` for each router in `main.py` / `app.py`
- Keep route handlers thin — delegate business logic to injected services
- Group request/response models with the router module they serve

## Red Flags — Stop and Correct
- Blocking I/O call inside an `async def` route handler
- Pydantic v1 `class Config:` or `@validator` instead of v2 `model_config` / `field_validator`
- Service or DB connection constructed inside a route body instead of injected with `Depends()`
- All routes defined directly on `app` in `main.py` instead of split into `APIRouter`s
- Endpoint without a declared `response_model` returning raw ORM objects
- `prefix`/`tags` repeated on every endpoint instead of set once at the router
```

**argus/packs/fastapi/checklist.md** — 8-12 checkbox items derived from the rules above. Format `## FastAPI Checklist` header then `- [ ]` items. Example items: route handlers are `async def`; no blocking I/O inside async handlers; request/response Pydantic models declared; `Field()` used for validation; v2 `model_config`/`model_validator` (no v1 Config); shared resources injected via `Depends()`; one `APIRouter` per domain; `prefix`/`tags` set at router level; `response_model` declared per endpoint.

**argus/packs/fastapi/examples.md** — 3-4 Avoid/Prefer side-by-side blocks in the security/kotlin examples.md format (`## FastAPI Examples` header, `### <topic>` subsections, `**Avoid**` then `**Prefer**` fenced python blocks). Cover: (1) blocking call in async handler vs httpx await, (2) inline route in main vs APIRouter with prefix/tags, (3) constructing a service in the handler vs `Depends()` injection, (4) Pydantic v1 validator vs v2 `field_validator`. Do NOT include any python-pack content (no f-string/pathlib/PEP 8 examples).

After writing all four files, run the fastapi tests — they MUST pass (GREEN). Then run the full suite to confirm no regressions.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/integration/test_generate.py -k fastapi -x -q && .venv/bin/pytest -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `argus/packs/fastapi/pack.yml` contains `category: framework` and `name: fastapi`
    - `grep -c "^## " argus/packs/fastapi/instructions.md` returns 5 (4 content sections + Red Flags)
    - `grep "APIRouter" argus/packs/fastapi/instructions.md` matches
    - `grep "Red Flags — Stop and Correct" argus/packs/fastapi/instructions.md` matches
    - instructions.md does NOT contain forbidden python-pack strings: `grep -E "mypy|PEP 8|f-string|pathlib" argus/packs/fastapi/instructions.md` returns nothing
    - instructions.md does NOT contain forbidden general-Pydantic strings: `grep -E "BaseSettings|discriminated union" argus/packs/fastapi/instructions.md` returns nothing
    - `argus/packs/fastapi/examples.md` contains `**Prefer**` and `**Avoid**`
    - `argus/packs/fastapi/checklist.md` has at least 8 `- [ ]` items
    - `.venv/bin/pytest tests/integration/test_generate.py -k fastapi -x -q` exits 0 (GREEN)
    - `.venv/bin/pytest -x -q` exits 0 (no regressions)
  </acceptance_criteria>
  <done>The fastapi pack directory has all 4 files, all 3 fastapi tests pass, and the full suite is green.</done>
</task>

</tasks>

<verification>
- `.venv/bin/pytest tests/integration/test_generate.py -k fastapi -x -q` — 3 tests pass
- `.venv/bin/pytest -x -q` — full suite green
- `argus packs list` includes fastapi under framework category
- `argus packs show fastapi` renders instructions containing APIRouter
</verification>

<success_criteria>
- argus/packs/fastapi/ contains pack.yml, instructions.md, checklist.md, examples.md
- instructions.md has 4 content sections + Red Flags closer; contains APIRouter
- No duplication of python-pack or general-Pydantic content
- tests/test_packs.py unchanged
- All fastapi integration tests pass; full suite green
</success_criteria>

<output>
After completion, create `.planning/phases/23-python-javascript-framework-packs/23-01-SUMMARY.md`
</output>
