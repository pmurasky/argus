---
phase: 23-python-javascript-framework-packs
plan: 02
type: tdd
wave: 2
depends_on: [01]
files_modified:
  - tests/integration/test_generate.py
  - argus/packs/nextjs/pack.yml
  - argus/packs/nextjs/instructions.md
  - argus/packs/nextjs/checklist.md
  - argus/packs/nextjs/examples.md
autonomous: true
requirements: [FWRK-02]

must_haves:
  truths:
    - "User can add `nextjs` to .argus.yml packs and `argus generate` injects Next.js rules into .claude/rules/nextjs.md"
    - "`argus packs list` shows nextjs under the framework category"
    - "`argus packs show nextjs` renders the nextjs instructions content"
  artifacts:
    - path: "argus/packs/nextjs/pack.yml"
      provides: "nextjs pack manifest with category: framework"
      contains: "category: framework"
    - path: "argus/packs/nextjs/instructions.md"
      provides: "Next.js rules across 4 sections; contains key phrase use client"
      contains: "use client"
    - path: "argus/packs/nextjs/checklist.md"
      provides: "Pre-commit checklist for Next.js"
      min_lines: 8
    - path: "argus/packs/nextjs/examples.md"
      provides: "Avoid/Prefer side-by-side Next.js examples"
      contains: "**Prefer**"
    - path: "tests/integration/test_generate.py"
      provides: "NEXTJS_CONFIG + 3 integration tests"
      contains: "NEXTJS_CONFIG"
  key_links:
    - from: "tests/integration/test_generate.py"
      to: "argus/packs/nextjs/instructions.md"
      via: "packs show / generate assert key phrase use client"
      pattern: "use client"
    - from: "argus/packs/nextjs/pack.yml"
      to: "argus.loader auto-discovery"
      via: "directory under argus/packs/ discovered by available_packs()"
      pattern: "name: nextjs"
---

<objective>
Ship the `nextjs` framework pack: a four-file pack directory under `argus/packs/nextjs/` plus
three integration tests in `tests/integration/test_generate.py`. The pack injects Next.js App
Router-specific rules (App Router conventions, server vs client components, React hooks
discipline, TypeScript integration) into agent instruction files.

Purpose: Satisfies FWRK-02 — users building Next.js 14+ App Router apps get precise agent rules.
Output: `argus/packs/nextjs/{pack.yml,instructions.md,checklist.md,examples.md}` and 3 passing
integration tests following the KOTLIN_CONFIG precedent.
</objective>

<execution_context>
@/home/peter/.claude/get-shit-done/workflows/execute-plan.md
@/home/peter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-python-javascript-framework-packs/23-CONTEXT.md
@.planning/phases/23-python-javascript-framework-packs/23-RESEARCH.md

# Reference packs (structural template — copy structure, replace content)
@argus/packs/typescript/pack.yml
@argus/packs/kotlin/instructions.md
@argus/packs/kotlin/checklist.md
@argus/packs/security/examples.md

# Test pattern (replicate KOTLIN_CONFIG block verbatim, swap nextjs)
@tests/integration/test_generate.py

<interfaces>
<!-- How the pack system works — executor needs no codebase exploration. -->

pack.yml manifest format (consumed by argus/loader.py PackLoader._read()):
```yaml
name: nextjs
description: <one line>
category: framework
requires: []
platforms: [all]
```

`argus packs show nextjs` renders instructions.md ONLY (the `instructions` field).
The key phrase asserted by the show + generate tests MUST appear verbatim in instructions.md
(NOT in checklist.md or examples.md). Chosen key phrase: use client.

`argus generate` with packs: [nextjs], platforms: [claude] writes pack content to
`.claude/rules/nextjs.md` in the project root.

Loader auto-discovers any directory under `argus/packs/` — no registration, no code changes.

NOTE on key phrase: the assert is `"use client" in output`. Writing the directive as `'use client'`
(with single quotes) in instructions.md still contains the substring `use client`, so the assert
passes. Confirm the bare substring `use client` is present.
</interfaces>

<constraints>
- Test runner is `.venv/bin/pytest` (NOT bare pytest).
- DO NOT modify tests/test_packs.py — its VALID_CATEGORIES and REQUIRED_PACKS must not change.
- This plan depends on Plan 01: it appends to test_generate.py AFTER the fastapi block. Read the
  current end of the file before appending so you place the NEXTJS block after the fastapi tests.
- Next.js pack is ADDITIVE — it must NOT duplicate typescript pack content. FORBIDDEN strings in
  instructions.md: noImplicitAny, "strict mode", "no-any" (those belong to typescript pack).
- App Router ONLY. FORBIDDEN: pages/, getServerSideProps, getStaticProps, getInitialProps,
  "Pages Router" (legacy — out of scope per CONTEXT.md).
- Next.js 14+ App Router conventions: app/ directory with layout.tsx, page.tsx, loading.tsx, error.tsx.
- Hooks discipline: hooks require `'use client'`; no hooks in Server Components;
  useCallback/useMemo only for a measured performance need (not by default).
</constraints>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add NEXTJS_CONFIG and 3 failing integration tests (RED)</name>
  <files>tests/integration/test_generate.py</files>
  <read_first>
    - tests/integration/test_generate.py (read the END of the file — after Plan 01 it ends with the fastapi tests; append the nextjs block after them, mirroring the KOTLIN_CONFIG/FASTAPI_CONFIG pattern)
  </read_first>
  <behavior>
    - test_nextjs_pack_appears_in_packs_list: `packs list` exit 0 and output contains "nextjs"
    - test_nextjs_pack_show_renders_content: `packs show nextjs` exit 0 and output contains "use client"
    - test_nextjs_pack_generate_injects_content: generate with NEXTJS_CONFIG writes
      .claude/rules/nextjs.md containing "use client"
    All 3 MUST fail initially (pack directory does not exist yet).
  </behavior>
  <action>
Append to the END of tests/integration/test_generate.py (after the fastapi tests added in Plan 01) the following block, replicating the established pattern exactly:

```python


NEXTJS_CONFIG = """\
packs:
  - nextjs
platforms:
  - claude
"""


def test_nextjs_pack_appears_in_packs_list():
    """Given packs list is invoked, nextjs appears in the output."""
    runner = CliRunner()
    result = runner.invoke(main, ["packs", "list"])
    assert result.exit_code == 0
    assert "nextjs" in result.output


def test_nextjs_pack_show_renders_content():
    """Given packs show nextjs is invoked, use client appears in the output."""
    runner = CliRunner()
    result = runner.invoke(main, ["packs", "show", "nextjs"])
    assert result.exit_code == 0
    assert "use client" in result.output


def test_nextjs_pack_generate_injects_content(tmp_path):
    """Given a nextjs+claude config, generate writes content to .claude/rules/nextjs.md."""
    (tmp_path / ".argus.yml").write_text(NEXTJS_CONFIG)
    runner = CliRunner()
    result = runner.invoke(main, ["generate", "--project-root", str(tmp_path)])
    assert result.exit_code == 0, result.output
    assert "use client" in (tmp_path / ".claude/rules/nextjs.md").read_text()
```

Do NOT add nextjs to FULL_CONFIG or any shared constant. Do NOT touch tests/test_packs.py.

Run the tests and CONFIRM they fail (RED) because argus/packs/nextjs/ does not exist yet.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/integration/test_generate.py -k nextjs -x -q; test $? -ne 0 && echo "RED confirmed"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "NEXTJS_CONFIG" tests/integration/test_generate.py` returns at least 2
    - `grep -c "def test_nextjs_pack" tests/integration/test_generate.py` returns 3
    - `grep "use client" tests/integration/test_generate.py` matches (key phrase in show + generate asserts)
    - `git diff --name-only` does NOT include tests/test_packs.py
    - `.venv/bin/pytest tests/integration/test_generate.py -k nextjs -x -q` exits NON-zero (RED — pack not authored yet)
  </acceptance_criteria>
  <done>3 nextjs tests exist in test_generate.py and all fail because the pack directory is absent.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author the nextjs pack files (GREEN)</name>
  <files>argus/packs/nextjs/pack.yml, argus/packs/nextjs/instructions.md, argus/packs/nextjs/checklist.md, argus/packs/nextjs/examples.md</files>
  <read_first>
    - argus/packs/typescript/pack.yml (manifest structure to mirror)
    - argus/packs/kotlin/instructions.md (4-section H2 density + "Red Flags — Stop and Correct" closer)
    - argus/packs/kotlin/checklist.md (checklist format)
    - argus/packs/security/examples.md (Avoid/Prefer side-by-side format)
  </read_first>
  <action>
Create the directory argus/packs/nextjs/ with four files.

**argus/packs/nextjs/pack.yml** (verbatim):
```yaml
name: nextjs
description: App Router conventions, server components, and React hooks discipline
category: framework
requires: []
platforms: [all]
```

**argus/packs/nextjs/instructions.md** — 4 H2 sections, 4-6 bullet rules each, ending with a "## Red Flags — Stop and Correct" section. The bare substring `use client` MUST appear (write the directive as `'use client'`). Write concrete, App Router-specific rules. Use exactly these four sections:

```markdown
# Next.js

## App Router Conventions
- Use the `app/` directory exclusively — App Router only, never the legacy `pages/` directory
- Map routes to folders: each route segment is a folder with a `page.tsx`
- Use `layout.tsx` for shared UI that persists across child routes (nav, providers)
- Use `loading.tsx` for route-level Suspense fallbacks and `error.tsx` for route error boundaries
- Co-locate route-specific components, but keep `page.tsx` thin and composed of smaller components
- Fetch data in Server Components with `async` components and `await` directly — no `useEffect` data fetching

## Server vs Client Components
- Default to Server Components — they render on the server and ship zero JS for that subtree
- Add the `'use client'` directive at the top of a file ONLY when it needs interactivity, state, or browser APIs
- Keep `'use client'` boundaries as low in the tree as possible to minimize client bundle size
- Never import a Server Component into a Client Component; pass it as `children` instead
- Mark server-only mutation functions with the `'use server'` directive (Server Actions)

## React Hooks Discipline
- Any file using a hook (`useState`, `useEffect`, `useRef`, custom hooks) MUST declare `'use client'`
- Never call hooks in a Server Component — Server Components do not re-render on the client
- Add `useCallback` / `useMemo` ONLY when there is a measured performance need, not by default
- Follow the rules of hooks: call them at the top level, never inside conditions or loops
- Prefer Server Components and Server Actions over client-side state where the work can run on the server

## TypeScript Integration
- Type route params and search params from the props Next.js passes to `page.tsx`
- Define explicit prop interfaces for every component (Server and Client)
- Type Server Action arguments and return values; never accept untyped `FormData` without parsing
- Use `Metadata` / `generateMetadata` typed exports for SEO instead of ad-hoc head tags
- Let async Server Components return typed data — the component is `async` and returns JSX

## Red Flags — Stop and Correct
- `pages/` directory, `getServerSideProps`, or `getStaticProps` (Pages Router — not allowed here)
- A hook used in a file that lacks the `'use client'` directive
- `'use client'` placed at the root layout, forcing the whole app into the client bundle
- Data fetched with `useEffect` in a component that could be an async Server Component
- `useCallback` / `useMemo` added speculatively without a measured performance need
- A Server Component imported into a Client Component instead of passed as `children`
```

**argus/packs/nextjs/checklist.md** — 8-12 checkbox items derived from the rules above. Format `## Next.js Checklist` header then `- [ ]` items. Example items: routes live under `app/` not `pages/`; `layout.tsx`/`loading.tsx`/`error.tsx` used where appropriate; default to Server Components; `'use client'` only where interactivity is needed; `'use client'` boundary kept low in the tree; every hook file declares `'use client'`; no hooks in Server Components; `useCallback`/`useMemo` only with a measured need; component prop interfaces defined; route params typed.

**argus/packs/nextjs/examples.md** — 3-4 Avoid/Prefer side-by-side blocks in the security/kotlin examples.md format (`## Next.js Examples` header, `### <topic>` subsections, `**Avoid**` then `**Prefer**` fenced tsx blocks). Cover: (1) `useEffect` fetch in client vs async Server Component fetch, (2) `'use client'` at root layout vs pushed down to a leaf interactive component, (3) hook used without `'use client'` vs with the directive, (4) speculative `useMemo` vs removing it. Do NOT include any typescript-pack content (no strict-mode/noImplicitAny/generics-basics examples).

After writing all four files, run the nextjs tests — they MUST pass (GREEN). Then run the full suite to confirm no regressions.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/integration/test_generate.py -k nextjs -x -q && .venv/bin/pytest -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `argus/packs/nextjs/pack.yml` contains `category: framework` and `name: nextjs`
    - `grep -c "^## " argus/packs/nextjs/instructions.md` returns 5 (4 content sections + Red Flags)
    - `grep "use client" argus/packs/nextjs/instructions.md` matches
    - `grep "Red Flags — Stop and Correct" argus/packs/nextjs/instructions.md` matches
    - instructions.md does NOT contain forbidden typescript-pack strings: `grep -E "noImplicitAny|strict mode|no-any" argus/packs/nextjs/instructions.md` returns nothing
    - instructions.md does NOT contain forbidden Pages Router strings: `grep -E "getServerSideProps|getStaticProps|getInitialProps" argus/packs/nextjs/instructions.md` returns nothing
    - `argus/packs/nextjs/examples.md` contains `**Prefer**` and `**Avoid**`
    - `argus/packs/nextjs/checklist.md` has at least 8 `- [ ]` items
    - `.venv/bin/pytest tests/integration/test_generate.py -k nextjs -x -q` exits 0 (GREEN)
    - `.venv/bin/pytest -x -q` exits 0 (no regressions)
  </acceptance_criteria>
  <done>The nextjs pack directory has all 4 files, all 3 nextjs tests pass, and the full suite is green.</done>
</task>

</tasks>

<verification>
- `.venv/bin/pytest tests/integration/test_generate.py -k nextjs -x -q` — 3 tests pass
- `.venv/bin/pytest -x -q` — full suite green
- `argus packs list` includes nextjs under framework category
- `argus packs show nextjs` renders instructions containing use client
</verification>

<success_criteria>
- argus/packs/nextjs/ contains pack.yml, instructions.md, checklist.md, examples.md
- instructions.md has 4 content sections + Red Flags closer; contains use client
- No duplication of typescript-pack content; no Pages Router content
- tests/test_packs.py unchanged
- All nextjs integration tests pass; full suite green
</success_criteria>

<output>
After completion, create `.planning/phases/23-python-javascript-framework-packs/23-02-SUMMARY.md`
</output>
